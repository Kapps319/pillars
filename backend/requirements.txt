# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Data / persistence ---
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.1,<4.0

# --- Validation / settings ---
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
email-validator>=2.1

# --- Background jobs ---
celery>=5.3,<6.0
redis>=5.0,<6.0

# --- Auth / crypto ---
PyJWT>=2.8,<3.0
bcrypt>=4.0,<5.0
cryptography>=42.0

# --- HTTP clients / scraping ---
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0

# --- LLM providers ---
# Official Anthropic SDK (used only when LLM_PROVIDER=claude and ANTHROPIC_API_KEY is set)
anthropic>=0.40

# --- Misc ---
PyYAML>=6.0
python-dateutil>=2.9
